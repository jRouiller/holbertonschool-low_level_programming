#include "main.h"
#include <stdio.h>

/**
 * _isupper - main
 *
 * Return: return
 */

int _isupper(int c)
{
	/*this should return if letter is a capital letter or not*/
	if (c >= 'A' && c <= 'Z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
